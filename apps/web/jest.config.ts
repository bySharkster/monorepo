/**
 * Jest configuration for the web application.
 *
 * @property {string} preset - Specifies the preset configuration for ts-jest.
 * @property {string} testEnvironment - Defines the test environment to be used, in this case, Node.js.
 */
module.exports = {
  preset: "ts-jest",
  testEnvironment: "node",
};
